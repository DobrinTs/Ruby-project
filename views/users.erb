<% if @users.empty? %>
    В момента регистрирани потребители.
<% end %>
<% @users.each do |user| %>
    <p>
        <%= user.name %>
        <%= user.password %>
        <%= user.email %>
        <%= user.rank %>
        
    </p>
<% end %>