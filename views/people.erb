<h1 align="center">Хора</h1>

<% if flash[:success] %>
    <p class="bg-success"><%= flash[:success] %></p>
<% end %>

<% if flash[:error] %>
    <p class="bg-danger"><%= flash[:error] %></p>
<% end %>

<form method="get" action="/create_person">
    <button type="submit" class="btn btn-default">Добавяне на нов човек/редактиране на съществуващ</button>
</form>

<form method="get" action="/delete_person">
    <button type="submit" class="btn btn-default">Изтриване на човек</button>
</form>

<form method="get" action="/movies">
    <button type="submit" class="btn btn-default">Отиди на страница с филми</button>
</form>

<h2 align="center">Съществуващи хора:</h2>

<% if @people.empty? %>
    Все още няма добавени хора ;/
<% end %>
<% @people.each do |person| %>
    <p>
        <% @person = person %>
        <a href="/person/<%= @person.name %>"><%= @person.name %></a>    
    </p>
<% end %>
